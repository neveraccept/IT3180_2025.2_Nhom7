package org.example.backend.entity.enums;

public enum ParkingRegistrationStatus {
    ACTIVE, // Đang hiệu lực (đang chiếm chỗ)
    ENDED   // Đã kết thúc (hủy đăng ký / hết hạn thuê), chỗ được giải phóng
}
